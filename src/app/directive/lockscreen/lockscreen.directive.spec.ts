import { LockscreenDirective } from './lockscreen.directive';

describe('LockscreenDirective', () => {
  it('should create an instance', () => {
    const directive = new LockscreenDirective();
    expect(directive).toBeTruthy();
  });
});
