import { BootupDirective } from './bootup.directive';

describe('BootupDirective', () => {
  it('should create an instance', () => {
    const directive = new BootupDirective();
    expect(directive).toBeTruthy();
  });
});
